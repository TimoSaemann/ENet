I have already trained ENet on the fine labeled Cityscapes dataset (2975 images) with 19 classes.

To download this weights run `sh cityscapes_weights.sh`. 

Further trained weights will be released soon.
